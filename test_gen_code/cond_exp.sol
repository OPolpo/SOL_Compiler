--test :P
func tester(w:int;):int
var i, j, k: int;
 a, b, c: bool;
begin tester
	w = if a or b then 
		i + 48
	elsif c and j > 20 then
		j - k
	elsif i != j / k then 
		k * i
	else
	36
	endif;
	return w;
end tester