-- no sense
func f2(): int 
var a,b,c:int;
	d,e,f:real;
	ff:bool;

	begin f2

		ff = false or rd bool;
--		if(rd bool) then
--			write toreal(toint(rd real));
--		else
--			if(rd bool) then
--				if(rd bool) then
--					if rd bool then
--						write if(rd bool) then "vero" else "falso" endif;
--					endif;
--				endif;
--			endif;	
--		endif;
		write "\n";
		return 0;
	end f2
