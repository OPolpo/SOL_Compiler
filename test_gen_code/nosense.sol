-- no sense
func f2(): int 
var a,b,c:int;
	d,e,f:real;
	ff:bool;
	i:int;

	begin f2

--		ff = false or rd bool;
--		for i=0 to 5 do
--			for i=0 to 2 do
--				write "ciao";
--			endfor;
--		endfor; 
--		if(rd bool) then
--			write toreal(toint(rd real));
--		else
--			if(rd bool) then
--				if(rd bool) then
--					if rd bool then
--						write if(rd bool) then "vero" else "falso" endif;
--					endif;
--				endif;
--			endif;	
--		endif;
		write "\n";
		return 0;
	end f2
