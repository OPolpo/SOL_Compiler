--test :P
func tester2(prova: vector[10] of struct(a:int; b: vector[20] of vector[5] of real;);):int
var c, d: bool;
	temp: int;
	o: int ;
	a:int;

	m: bool;
	b: vector[10] of vector[10] of real;
func test(a,b: int;):int
var t,r,c:int;
	w:struct(s:int; ss:string;);
begin test
	if a==b then
		t = r;
		r = t;
	endif;

	if a==b then
		t = r;
		r = t;
	elsif a>b then
		w = struct(2, "alpha");
		a = b + c;
	else
		a = b-c;
		c = 1;
	endif;
end test

begin tester2
	--temp = prova[1].a;
	--d = (temp == 2);
	--c = c and (prova[1].b[1][1] == 3.0);
	--c = c or (b[1][1] <= 2.0);
	--c = (c == d) and (d!= c); 
	m = c and d or a > 4;
	o = 3+2*1;
	o = 3*2+1;

	return -pippo88878y65cytrdxctrctrct7;
	return p();
	return ppp(ll);
	--a[o]=funzione(parametro1, parametro2, 0 , e[o], t.o);

	o = 0;
	


		k = - i;
	g = -2;
	f = -(4+3-2);
end tester2